name: Visuelstory

on:
  push:
    branches: ["dev"]
  pull_request:
    branches: ["dev"]

jobs:
  web-build-job:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"
